# SunEd Backup

This repository currently tracks the React-based SunEd landing page implementation. It does not have a Git remote configured, so no commits have been pushed to GitHub from this environment.

## Checking remote configuration

To verify whether a Git remote exists for this checkout, run:

```bash
git remote -v
```

If the command produces no output, as in this workspace, the repository is not connected to any remote and nothing has been pushed.
